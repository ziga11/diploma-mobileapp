name: diplomaapp
description: "Diploma Flutter Project"
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  path: ^1.9.0
  path_provider: ^2.1.4
  firebase_messaging: ^16.1.0
  firebase_core: ^4.3.0
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.3.2
  syncfusion_flutter_pdfviewer: ^32.1.23
  intl: ^0.20.2
  url_launcher: ^6.3.0
  app_links: ^7.0.0
  pdf: ^3.11.1
  html: ^0.15.4
  file_picker: ^10.2.0
  flutter_local_notifications: ^19.2.1
  country_picker: ^2.0.26
  http_parser: ^4.0.2
  logger: ^2.5.0
  webview_flutter_android: ^4.7.0
  flutter_svg: ^2.2.1
  auto_size_text: ^3.0.0
  xml: ^6.6.1
  flutter_file_downloader: ^2.1.1
  get_it: ^9.2.0
  scrollable_positioned_list: ^0.3.8
  open_file: ^3.5.11
  permission_handler: ^12.0.1
  device_info_plus: ^12.3.0
  docx_viewer: ^1.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  rename: ^3.1.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/
    - assets/Menu_Icons/
    - assets/translations/

  #   - assets/a_dot_burr.jpeg
  #   - assets/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Montserrat
      fonts:
        - asset: assets/Montserrat/Montserrat-Black.ttf
          weight: 900
          style: normal
        - asset: assets/Montserrat/Montserrat-BlackItalic.ttf
          weight: 900
          style: italic
        - asset: assets/Montserrat/Montserrat-Bold.ttf
          weight: 700
          style: normal
        - asset: assets/Montserrat/Montserrat-BoldItalic.ttf
          weight: 700
          style: italic
        - asset: assets/Montserrat/Montserrat-ExtraBold.ttf
          weight: 800
          style: normal
        - asset: assets/Montserrat/Montserrat-ExtraBoldItalic.ttf
          weight: 800
          style: italic
        - asset: assets/Montserrat/Montserrat-ExtraLight.ttf
          weight: 200
          style: normal
        - asset: assets/Montserrat/Montserrat-ExtraLightItalic.ttf
          weight: 200
          style: italic
        - asset: assets/Montserrat/Montserrat-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/Montserrat/Montserrat-Light.ttf
          weight: 300
          style: normal
        - asset: assets/Montserrat/Montserrat-LightItalic.ttf
          weight: 300
          style: italic
        - asset: assets/Montserrat/Montserrat-Medium.ttf
          weight: 500
          style: normal
        - asset: assets/Montserrat/Montserrat-MediumItalic.ttf
          weight: 500
          style: italic
        - asset: assets/Montserrat/Montserrat-Regular.ttf
          weight: 400
          style: normal
        - asset: assets/Montserrat/Montserrat-SemiBold.ttf
          weight: 600
          style: normal
        - asset: assets/Montserrat/Montserrat-SemiBoldItalic.ttf
          weight: 600
          style: italic
        - asset: assets/Montserrat/Montserrat-Thin.ttf
          weight: 100
          style: normal
        - asset: assets/Montserrat/Montserrat-ThinItalic.ttf
          weight: 100
          style: italic

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
